Copy the example configuration file before running and add actual credentials:

cp application-example.properties application.properties

Steps to run:

run AuthappApplication in auth-backend

npm start in auth-frontend